# Results

Empty, deliberately.

No comparative run has happened. When one does, this directory will hold the per-run JSONL and the
full transcript of every run, exactly as produced — not a summary. If a summary ever disagrees with
what is in here, the files are right and the summary is a bug.

The task set was pre-registered and tagged before any harness was run against it:

    git show kolkbench-tasks-v1
